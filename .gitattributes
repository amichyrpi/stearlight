# The image is assembled by Linux tooling; keep every OS recipe/configuration
# byte-for-byte Linux compatible when the repository is checked out on Windows.
/os/** text eol=lf
*.mkv binary
*.mp4 binary
*.png binary
